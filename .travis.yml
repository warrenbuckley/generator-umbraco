language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-umbraco' ]; then cd .. && eval "mv $currentfolder generator-umbraco" && cd generator-umbraco; fi

